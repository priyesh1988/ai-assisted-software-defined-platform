
version: '3'
services:
  api:
    image: python:3.11
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "pip install -r requirements.txt && uvicorn control_plane.api:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
